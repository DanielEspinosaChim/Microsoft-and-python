import sys #estas importando la libreria sys
print(sys.argv)
print(sys.argv[0]) # program name
print(sys.argv[1]) # first arg sys.argves una matriz o una estructura de datos que contiene muchos elementos. La primera posición, indicada como 0en la matriz, contiene el nombre del programa. La segunda posición, 1contiene su primer argumento. Suponga que el programa backup.py contiene el código de muestra y lo ejecuta así: